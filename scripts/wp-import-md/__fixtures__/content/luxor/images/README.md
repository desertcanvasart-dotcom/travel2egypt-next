# Fixture image stub

`valley-of-the-kings-hero.jpg` in this directory is a tiny synthetic JPEG used
by the unit + integration tests to exercise the image-upload path without
relying on real photographic content.

It is committed deliberately as a binary fixture.
