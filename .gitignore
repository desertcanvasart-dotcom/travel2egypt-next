node_modules
.next
.env
.env.*
!.env.example
.DS_Store
*.log
.sanity
dist
.vercel
tsconfig.tsbuildinfo
assets/
migration/.cache/
migration/.brand-inputs/
.claude/
!.claude/settings.json
migration/content/*-prep.md
migration/content/about-team-photo.*
migration/content/*.csv
migration/content/*.dmg
backups/
.cache/

# Root-level one-off analysis artifacts (image audits, worklists, remaps).
# These are throwaway exports from ad-hoc sessions, not source — ignore so
# they stop cluttering `git status`. The reusable scanner scripts under
# scripts/ are NOT covered by these patterns and remain trackable.
/*-worklist.csv
/*-worklist.xlsx
/*-audit.csv
/jap166-*
/images-under-3000px.csv
/orphan-under-3000.csv
/tour-image-match.csv
/untouched-tours.csv
/giza-audit.csv
scripts/concierge-eval/results/

# Regenerable data dumps written by tracked scripts into scripts/ —
# outputs, not source (migrate-goodfor-and-shape.ts, audit-raw-links-*.cjs,
# raw-link-recheck-*.cjs). Re-running the script recreates them.
scripts/.goodfor-shape-plan.json
scripts/raw-links-remaining.json
scripts/raw-link-recheck-*.json
