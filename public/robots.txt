# Travel2Egypt — robots.txt
# Sitemap reference; AI-crawler stance below.

User-agent: *
Allow: /
Disallow: /studio
Disallow: /studio/
Disallow: /api/

# ── AI crawlers ──────────────────────────────────────────
# Listed explicitly so future-you doesn't have to guess what was decided.
# Current stance: ALLOW. The brand wants citation discoverability — the
# editorial content is a strategic asset and being cited by LLMs is the
# distribution channel we are building toward (see llms.txt).
#
# To opt out of AI training (without blocking on-demand fetchers),
# replace each `Allow: /` below with `Disallow: /`.

# Anthropic — Claude training crawler
User-agent: anthropic-ai
Allow: /

# Anthropic — Claude on-demand fetcher (when a user asks Claude about a URL)
User-agent: ClaudeBot
Allow: /

# OpenAI — ChatGPT training crawler
User-agent: GPTBot
Allow: /

# OpenAI — ChatGPT on-demand fetcher (when a ChatGPT user references a URL)
User-agent: ChatGPT-User
Allow: /

# Google — used by Bard / Gemini training (separate from Googlebot)
User-agent: Google-Extended
Allow: /

# Perplexity
User-agent: PerplexityBot
Allow: /

# Common Crawl (the corpus most LLMs train from)
User-agent: CCBot
Allow: /

Sitemap: https://travel2egypt.org/sitemap.xml
